import React from 'react';
import { Icon, Popover } from 'antd';
import { Car } from '../types';

interface Props {
}

export default class CarFilter extends React.Component<Props> {
    render() {
      return (
          <div>ayy</div>
      );
    }
}